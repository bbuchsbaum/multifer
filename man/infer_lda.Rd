% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infer_wrappers.R
\name{infer_lda}
\alias{infer_lda}
\title{Infer discriminant-root significance for LDA-family models}
\usage{
infer_lda(
  X,
  labels,
  targets = "component_significance",
  adapter = "lda_refit",
  ...
)
}
\arguments{
\item{X}{Numeric matrix with observations in rows and variables in columns.}

\item{labels}{Factor of class labels with one entry per row of \code{X}.}

\item{targets}{Requested inferential targets. Defaults to
\code{"component_significance"} because the geneig bootstrap/stability
path is not yet part of the public wrapper surface.}

\item{adapter}{Adapter id or object. Defaults to \code{"lda_refit"}.}

\item{...}{Additional arguments forwarded to \code{\link[=infer]{infer()}}.}
}
\value{
An \link{infer_result}.
}
\description{
Thin convenience wrapper over \code{\link[=infer]{infer()}} for \verb{(geneig, generalized_eigen)} models driven by class labels. The default adapter
is \code{"lda_refit"}, which wraps \code{MASS::lda()} and tests the ordered
discriminant roots, not specific variables and not pairwise class
contrasts.
}
\details{
The estimand is the number of discriminant roots that rise above
noise. The ladder is therefore naturally short: with \code{K} classes,
there are at most \code{K - 1} non-zero discriminant roots to test.

For the current engine doctrine and the B-metric deflation rule, see
\code{notes/engine_geneig_spec.md}.
}
