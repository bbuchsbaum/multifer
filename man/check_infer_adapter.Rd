% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter_check.R
\name{check_infer_adapter}
\alias{check_infer_adapter}
\title{Check an adapter against its declared inference contract}
\usage{
check_infer_adapter(
  adapter,
  fixture,
  targets = "default",
  geometry = NULL,
  relation = NULL,
  run = c("smoke", "compile"),
  B = 19L,
  R = 3L,
  seed = 1L,
  strict = TRUE,
  fail_on_error = FALSE,
  ...
)
}
\arguments{
\item{adapter}{A \code{multifer_adapter} object or registered adapter id.}

\item{fixture}{Data fixture accepted by the adapter. This may be a single
data object used for every checked capability, or a named list containing
entries named \code{"geometry/relation"}, \code{"geometry"}, or \code{"default"}.}

\item{targets}{Character vector of targets to check, or \code{"default"} to
check every target declared for each capability pair except
\code{"variable_significance"}.}

\item{geometry}{Optional character vector limiting checked geometries.}

\item{relation}{Optional character vector limiting checked relations.}

\item{run}{One of \code{"smoke"} or \code{"compile"}. \code{"compile"} only compiles
recipes and runs validity checks. \code{"smoke"} also runs \code{infer()} with
small Monte Carlo and bootstrap budgets.}

\item{B}{Integer Monte Carlo budget passed to \code{infer()} in smoke mode.}

\item{R}{Integer bootstrap replicate count passed to \code{infer()} in smoke
mode.}

\item{seed}{Optional integer seed passed to \code{infer()} in smoke mode.}

\item{strict}{Logical passed to \code{infer_recipe()}, \code{run_adapter_checks()},
and \code{infer()}.}

\item{fail_on_error}{Logical. If \code{TRUE}, stop when any checked row fails.}

\item{...}{Additional arguments forwarded to \code{infer()} in smoke mode.}
}
\value{
A data frame of class \code{multifer_adapter_check} with one row per
checked capability target.
}
\description{
\code{check_infer_adapter()} is a smoke-test harness for adapter authors and
for \code{multifer}'s own engine refactors. It walks the adapter's declared
\code{(geometry, relation, target)} capability rows, compiles each requested
target as a recipe, runs the adapter's executable validity checks, and
optionally runs a small \code{infer()} call for that target.
}
\details{
The function is deliberately report-oriented: by default it returns every
row it checked, including failures, so a package author can see exactly
which declared capability is not executable. Set \code{fail_on_error = TRUE}
to turn any failed row into an error, which is useful in downstream tests.
}
