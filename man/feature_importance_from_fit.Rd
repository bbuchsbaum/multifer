% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature_importance.R
\name{feature_importance_from_fit}
\alias{feature_importance_from_fit}
\title{Feature importance from an adapter fit}
\usage{
feature_importance_from_fit(
  fit,
  adapter,
  units,
  data = NULL,
  k = c("selected", "all"),
  weights = c("equal", "root", "root2"),
  roots = NULL,
  normalize = c("block", "global", "none"),
  scope = c("aggregate", "unit", "both"),
  statistic = c("loadings", "adapter")
)
}
\arguments{
\item{fit}{Fitted adapter object.}

\item{adapter}{A \code{multifer_adapter}.}

\item{units}{A \code{multifer_units} table.}

\item{data}{Original data passed to \code{adapter$variable_stat()} when
\code{statistic = "adapter"}. The loading-based statistic does not use
this argument.}

\item{k}{Unit selection. One of \code{"selected"}, \code{"all"}, a
character vector of unit ids, or an integer vector of unit rows.}

\item{weights}{One of \code{"equal"}, \code{"root"}, \code{"root2"},
or a numeric vector matching either all units or the selected units.}

\item{roots}{Optional numeric vector of component roots used for
\code{"root"} and \code{"root2"} weights. If omitted and
\code{adapter$roots()} is available, roots are read from \code{fit}.}

\item{normalize}{One of \code{"block"}, \code{"global"}, or
\code{"none"}.}

\item{scope}{One of \code{"aggregate"}, \code{"unit"}, or \code{"both"}.}

\item{statistic}{One of \code{"loadings"} or \code{"adapter"}.}
}
\value{
A \code{multifer_feature_importance} data frame.
}
\description{
Compute observed feature-level importance from a single fitted adapter.
The default statistic is the same squared-loading statistic used by
\code{\link{feature_importance_from_bootstrap}()}. Adapters can instead
provide a family-specific \code{variable_stat()} hook and request
\code{statistic = "adapter"}.
}
