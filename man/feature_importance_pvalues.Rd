% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature_importance.R
\name{feature_importance_pvalues}
\alias{feature_importance_pvalues}
\title{Null-calibrated p-values for feature importance}
\usage{
feature_importance_pvalues(
  recipe,
  adapter,
  data,
  units,
  original_fit = NULL,
  B = 1000L,
  k = c("selected", "all"),
  weights = c("equal", "root", "root2"),
  roots = NULL,
  normalize = c("block", "global", "none"),
  adjust = c("maxT", "BH", "none"),
  seed = NULL
)
}
\arguments{
\item{recipe}{A \code{multifer_infer_recipe}.}

\item{adapter}{A \code{multifer_adapter}.}

\item{data}{Original row-aligned data.}

\item{units}{A \code{multifer_units} table.}

\item{original_fit}{Optional original fit. If \code{NULL}, the adapter
is refit on \code{data}.}

\item{B}{Positive integer number of null draws.}

\item{k, weights, roots, normalize}{Passed to the same importance
statistic used by \code{\link{feature_importance_from_bootstrap}()}.}

\item{adjust}{One of \code{"maxT"}, \code{"BH"}, or \code{"none"}.}

\item{seed}{Optional integer seed.}
}
\value{
A \code{multifer_feature_importance_pvalues} data frame.
}
\description{
Compute Monte Carlo p-values for unsigned aggregate feature importance
using an adapter's design-valid \code{null_action}. This is not a
bootstrap procedure.
}
\details{
This sidecar API uses the adapter's \code{domains()}, \code{loadings()},
\code{null_action()}, and \code{refit()} hooks. When a geometry's null
payload is not already shaped like the adapter's \code{refit()} input, the
adapter can provide a \code{refit_data()} hook to translate it.
}
