% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infer_problem.R
\name{infer_problem}
\alias{infer_problem}
\title{Compiled inferential problem object}
\usage{
infer_problem(
  shape,
  targets,
  adapter_id,
  adapter_version = NA_character_,
  validity_level,
  strict = TRUE,
  downgrade_reason = NA_character_,
  target_family = NULL,
  engine_kind = NULL,
  call = NULL
)
}
\arguments{
\item{shape}{A \code{multifer_typed_shape}.}

\item{targets}{Character vector of resolved inferential targets.}

\item{adapter_id}{Character scalar naming the adapter.}

\item{adapter_version}{Optional adapter version string.}

\item{validity_level}{One of \code{c("exact", "conditional", "asymptotic",
"heuristic")}.}

\item{strict}{Logical scalar recording whether strict dispatch was enforced.}

\item{downgrade_reason}{Optional character scalar. Use \code{NA_character_} when
no downgrade occurred.}

\item{target_family}{Optional character scalar. When \code{NULL}, derived from
the shape relation.}

\item{engine_kind}{Optional character scalar. When \code{NULL}, derived from the
shape and relation.}

\item{call}{Optional matched call or other provenance object.}
}
\value{
An object of class \code{multifer_infer_problem}.
}
\description{
\code{infer_problem()} is the first-class representation of the inferential
question that \pkg{multifer} has compiled from a typed shape and an adapter's
declared capabilities. It keeps the package's ontology in one inspectable
place rather than spreading it across recipe fields and dispatch locals.
}
\details{
The object records:
\itemize{
\item the typed shape being inferred on,
\item the requested inferential targets,
\item the target family (\code{"latent_root"} or \code{"predictive_gain"}),
\item the engine kind selected for execution,
\item the adapter identity and compiled validity level,
\item whether strict dispatch was enforced and whether any downgrade occurred.
}
}
