% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter_lda.R
\name{adapter_lda_refit}
\alias{adapter_lda_refit}
\title{multifer adapter for MASS::lda}
\usage{
adapter_lda_refit(adapter_id = "lda_refit", adapter_version = "0.0.1")
}
\arguments{
\item{adapter_id}{Character, default \code{"lda_refit"}.}

\item{adapter_version}{Character, default \code{"0.0.1"}.}
}
\value{
A \code{multifer_adapter}.
}
\description{
A Tier-1 refit adapter for the \verb{(geneig, generalized_eigen)} family
using \code{MASS::lda()} as the fitting backend. The adapter stores the
fitted \code{lda} object together with the corresponding generalized-eigen
operator pair \verb{(A, B)} where \code{A} is the between-class scatter and \code{B}
is the within-class scatter. Null resampling permutes class labels
only; rows are left in place.
}
