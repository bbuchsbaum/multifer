% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine_geneig.R
\name{run_geneig_ladder}
\alias{run_geneig_ladder}
\title{Run the generalized-eigen sequential deflation engine}
\usage{
run_geneig_ladder(
  recipe,
  operator,
  state = NULL,
  B = 1000L,
  B_total = NULL,
  batch_size = 32L,
  alpha = 0.05,
  max_steps = NULL,
  seed = NULL,
  auto_subspace = TRUE,
  tie_threshold = 0.01
)
}
\arguments{
\item{recipe}{A compiled \code{multifer_infer_recipe}. Must have geometry
\code{"geneig"} and relation \code{"generalized_eigen"}.}

\item{operator}{A \code{\link[=geneig_operator]{geneig_operator()}} object containing the operator
pair \verb{(A, B)}.}

\item{state}{Optional list carried through the ladder and handed to
\code{recipe$adapter$null_action()}. This is primarily for adapter-side
bookkeeping; when \code{state$factor_matrix} is supplied and \code{B = I},
the engine preserves that factor matrix through deflation so the
oneblock regression can be checked exactly.}

\item{B}{Per-rung cap on Monte Carlo draws. Default \code{1000L}.}

\item{B_total}{Optional integer global Monte Carlo budget shared
across ladder rungs. Defaults to \code{B * max_steps}.}

\item{batch_size}{Positive integer, Besag-Clifford batch size within
each rung. Default \code{32L}.}

\item{alpha}{Significance threshold. Default \code{0.05}.}

\item{max_steps}{Maximum number of ladder rungs. Defaults to
\code{min(length(roots_observed), 50L)}.}

\item{seed}{Integer or \code{NULL}.}

\item{auto_subspace}{Logical. When \code{TRUE} (default), near-tied roots
are bundled via \code{\link[=form_units]{form_units()}} \code{group_near_ties = TRUE}.}

\item{tie_threshold}{Positive numeric. Relative-gap threshold used
when \code{auto_subspace = TRUE}. Default \code{0.01}.}
}
\value{
A list with \code{units}, \code{component_tests}, \code{roots_observed}, and
\code{ladder_result}, mirroring \code{\link[=run_oneblock_ladder]{run_oneblock_ladder()}}.
}
\description{
Implements the test ladder for \verb{(geneig, generalized_eigen)} recipes
via the shared \code{\link[=ladder_driver]{ladder_driver()}}. At each rung the engine whitens the
current operator pair to
}
\details{
\verb{C = B^\{-1/2\} A B^\{-1/2\}}

computes the ordered generalized roots of \code{C}, evaluates the
tail-ratio statistic on the leading remaining root, and deflates in
the \code{B}-metric by removing the top eigendirection in the whitened
space. Null resampling is delegated to the adapter's \code{null_action()}
hook because valid nulls are sub-family-specific.
}
