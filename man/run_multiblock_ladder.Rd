% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine_multiblock.R
\name{run_multiblock_ladder}
\alias{run_multiblock_ladder}
\title{Run the generic multiblock sequential deflation engine}
\usage{
run_multiblock_ladder(
  recipe,
  adapter,
  data,
  B = 1000L,
  B_total = NULL,
  batch_size = 32L,
  alpha = 0.05,
  max_steps = NULL,
  seed = NULL,
  auto_subspace = TRUE,
  tie_threshold = 0.01
)
}
\arguments{
\item{recipe}{A compiled \code{multifer_infer_recipe} with geometry
\code{"multiblock"}.}

\item{adapter}{A \code{multifer_adapter} whose hooks support the recipe.}

\item{data}{A named or unnamed list of aligned numeric matrix blocks.}

\item{B}{Per-rung cap on Monte Carlo draws. Default \code{1000}.}

\item{B_total}{Optional integer global Monte Carlo budget shared
across ladder rungs. Defaults to \code{B * max_steps}.}

\item{batch_size}{Positive integer, Besag-Clifford batch size within
each rung. Default \code{32L}.}

\item{alpha}{Significance threshold. Default \code{0.05}.}

\item{max_steps}{Maximum number of ladder rungs. Defaults to the number
of roots returned by the adapter, capped at 50.}

\item{seed}{Integer or NULL.}

\item{auto_subspace}{Logical. When \code{TRUE} (default), near-tied
roots are automatically bundled into a subspace unit via
\code{\link[=form_units]{form_units()}} \code{group_near_ties = TRUE}.}

\item{tie_threshold}{Positive numeric. Relative-gap threshold used
when \code{auto_subspace = TRUE}. Default \code{0.01}.}
}
\value{
A list with elements \code{units}, \code{component_tests},
\code{roots_observed}, \code{ladder_result}, and \code{labels}.
}
\description{
Executes the latent-root ladder for adapters that declare
\code{geometry = "multiblock"}. Unlike the oneblock and cross reference
engines, this path is adapter-driven: the adapter owns refitting, null
generation, component statistics, and residualization. The engine only
supplies the common sequential testing loop and result assembly.
}
