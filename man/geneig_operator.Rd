% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geometry.R
\name{geneig_operator}
\alias{geneig_operator}
\title{Validated operator pair for a \code{geneig} problem}
\usage{
geneig_operator(A, B, metric = "b_metric")
}
\arguments{
\item{A}{A square, symmetric numeric matrix. Symmetry is checked to
a tolerance proportional to \code{max(abs(A)) * sqrt(.Machine$double.eps)}.}

\item{B}{A square, symmetric positive-definite numeric matrix of the
same dimension as \code{A}. Positive-definiteness is verified via a
Cholesky attempt; failure produces a specific error message
naming the failing pivot.}

\item{metric}{Character scalar labelling the metric that \code{B}
encodes (e.g. \code{"within_class"} for LDA, \code{"background_cov"} for
contrastive PCA). Purely descriptive — not consumed by the
engine. Default \code{"b_metric"}.}
}
\value{
An object of class \code{multifer_geneig_operator}, structurally
a list with fields \code{A}, \code{B}, \code{metric}, and \code{dim}.
}
\description{
Constructs a validated \verb{(A, B)} operator pair for a generalized
eigenvalue problem \verb{A v = lambda B v}. This is a data-side helper,
not a shape: it carries the symmetric operator \code{A} and the
symmetric positive-definite metric \code{B} plus optional metadata, and
validates them at construction time so the engine does not have to
re-check on every rung.
}
\details{
The matching typed shape is
\code{typed_shape(geometry("geneig"), relation("generalized_eigen"), design)}.

The constructor exists so that geneig adapters can accept a single
data argument and know statically that \code{A} and \code{B} have been
validated. See \code{notes/engine_geneig_spec.md} for the full engine
spec, including why the deflation is B-orthogonal rather than
Euclidean.
}
\examples{
A <- crossprod(matrix(stats::rnorm(30), 10, 3))
B <- crossprod(matrix(stats::rnorm(30), 10, 3)) + diag(3)
op <- geneig_operator(A, B)

}
