% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bench_generators.R
\name{bench_oneblock_null}
\alias{bench_oneblock_null}
\title{Generator: one-block null calibration}
\usage{
bench_oneblock_null(
  n = 200,
  p = 50,
  noise = c("gaussian", "heavy_tailed", "heteroscedastic"),
  seed = NULL
)
}
\arguments{
\item{n}{Integer. Number of observations (rows). Default 200.}

\item{p}{Integer. Number of variables (columns). Default 50.}

\item{noise}{Character scalar. Noise model: \code{"gaussian"} (standard
normal), \code{"heavy_tailed"} (t with df = 5), or \code{"heteroscedastic"}
(Gaussian with per-column variances drawn from an exponential).}

\item{seed}{Integer or NULL. RNG seed for reproducibility.}
}
\value{
A list with elements:
\describe{
\item{X}{Numeric matrix of dimensions n x p.}
\item{meta}{List with fields \code{n}, \code{p}, \code{noise}, \code{seed},
\code{true_rank} (always 0L).}
}
}
\description{
Generates a pure-noise dataset for testing over-selection rate and
p-value calibration in one-block inference. There is no real signal;
the true rank is zero.
}
