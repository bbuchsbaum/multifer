% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/capability_matrix.R
\name{capability_matrix}
\alias{capability_matrix}
\title{Build a capability matrix}
\usage{
capability_matrix(...)
}
\arguments{
\item{...}{Zero or more entry lists, each with fields \code{geometry}
(character scalar), \code{relation} (character scalar), and \code{targets}
(character vector of valid target names, see
\code{\link[=valid_capability_targets]{valid_capability_targets()}}).}
}
\value{
An object of class \code{multifer_capability_matrix}. Internally
a data.frame with columns \code{geometry}, \code{relation}, \code{target}.
}
\description{
A machine-readable grid of \verb{(geometry, relation, target) -> supported?} that every adapter declares. The two places it is
consulted are:
}
\details{
\enumerate{
\item \strong{Adapter registration.} An adapter cannot \emph{claim} a
\verb{(geometry, relation, target)} triple unless it provides the
hooks needed to compute it (Part 5 section 36, point 1). This
check is enforced by the adapter constructor in a later phase;
here we only define the data structure.
\item \strong{Recipe compilation.} \code{infer_recipe()} refuses to compile a
recipe whose requested target is not declared supported
(Part 5 section 36, point 2).
}

The constructor normalizes the input into a long-format data.frame
where each row is one supported \verb{(geometry, relation, target)}
triple. Geometry and relation values are validated against the
typed shape vocabulary: unknown values are rejected.
}
\examples{
cm <- capability_matrix(
  list(geometry = "oneblock", relation = "variance",
       targets = c("component_significance", "variable_stability")),
  list(geometry = "cross", relation = "covariance",
       targets = c("component_significance", "subspace_stability")),
  list(geometry = "cross", relation = "correlation",
       targets = "component_significance")
)
supports(cm, "cross", "correlation", "component_significance")
capabilities_for(cm, "cross", "covariance")

}
