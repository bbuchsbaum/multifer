% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature_evidence.R
\name{infer_feature_evidence}
\alias{infer_feature_evidence}
\title{Build the feature_evidence table}
\usage{
infer_feature_evidence(
  scope = character(0),
  domain = character(0),
  feature = character(0),
  unit_id = character(0),
  unit_set = character(0),
  statistic = character(0),
  estimate = numeric(0),
  bootstrap_mean = numeric(0),
  bias = numeric(0),
  std_error = numeric(0),
  lower = numeric(0),
  upper = numeric(0),
  z = numeric(0),
  z_label = character(0),
  p_value = numeric(0),
  p_adjusted = numeric(0),
  adjustment = character(0),
  calibration = character(0),
  interval_method = character(0),
  null_label = character(0),
  method = character(0),
  orientation = character(0),
  identifiable = logical(0),
  validity_level = character(0),
  warnings = character(0)
)
}
\arguments{
\item{scope}{Character vector. Typical values are \code{"unit"} and
\code{"aggregate"}.}

\item{domain}{Character vector of domains or block labels.}

\item{feature}{Character vector of feature identifiers.}

\item{unit_id}{Character vector of unit ids, or \code{NA} for aggregate rows.}

\item{unit_set}{Character vector describing the unit set.}

\item{statistic}{Character vector naming the statistic.}

\item{estimate, bootstrap_mean, bias, std_error, lower, upper, z}{Numeric
columns.}

\item{z_label}{Character vector describing \code{z}.}

\item{p_value, p_adjusted}{Numeric p-value columns. Use \code{NA} when no
null calibration was run.}

\item{adjustment}{Character vector naming the multiplicity adjustment.}

\item{calibration}{Character vector, one of \code{"bootstrap"},
\code{"null_refit"}, \code{"analytic"}, or \code{"none"}.}

\item{interval_method}{Character vector, one of \code{"percentile"},
\code{"normal"}, or \code{"none"}.}

\item{null_label}{Character vector describing the null.}

\item{method}{Character vector describing the evidence method.}

\item{orientation}{Character vector, one of \code{"signed"},
\code{"unsigned"}, \code{"subspace_norm"}, or \code{"adapter_invariant"}.}

\item{identifiable}{Logical vector.}

\item{validity_level}{Character vector, one of \code{"exact"},
\code{"conditional"}, \code{"asymptotic"}, or \code{"heuristic"}.}

\item{warnings}{Character vector with row-level caveats.}
}
\value{
A data frame with class \code{multifer_feature_evidence}.
}
\description{
\code{feature_evidence} is a sidecar table for feature-to-unit evidence and
aggregate feature importance. It is deliberately separate from
\code{variable_stability}: bootstrap ratios and null-calibrated p-values are
evidence summaries, not stability scores.
}
