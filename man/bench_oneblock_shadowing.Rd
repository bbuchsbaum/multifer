% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bench_generators.R
\name{bench_oneblock_shadowing}
\alias{bench_oneblock_shadowing}
\title{Generator: one-block shadowing (Vitale shadowing case)}
\usage{
bench_oneblock_shadowing(
  n = 200,
  p = 50,
  root_profile = c(8, 6, 4, 2, 1.2, 1.05),
  noise = c("gaussian", "heteroscedastic"),
  seed = NULL
)
}
\arguments{
\item{n}{Integer. Number of observations (rows). Default 200.}

\item{p}{Integer. Number of variables (columns). Default 50.}

\item{root_profile}{Numeric vector. Signal-to-noise ratio of each
true component. Default \code{c(8, 6, 4, 2, 1.2, 1.05)}.}

\item{noise}{Character scalar. \code{"gaussian"} or \code{"heteroscedastic"}.}

\item{seed}{Integer or NULL. RNG seed for reproducibility.}
}
\value{
A list with elements:
\describe{
\item{X}{Numeric matrix of dimensions n x p.}
\item{meta}{List with fields \code{n}, \code{p}, \code{noise}, \code{seed},
\code{true_rank} (\code{length(root_profile)}), \code{root_profile}.}
}
}
\description{
Generates a dataset where strong early roots hide weaker late roots.
This is the primary stress test for deflation: the weakest components
(lowest entries in \code{root_profile}) are the "shadowed" targets.
}
\details{
Data construction: \verb{X = U diag(root_profile) V^T + noise_matrix}
where \code{U} is a random orthonormal \verb{n x k} matrix, \code{V} is a random
orthonormal \verb{p x k} matrix, and \code{k = length(root_profile)}.
}
