% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infer_recipe.R
\name{infer_recipe}
\alias{infer_recipe}
\title{Compile a typed shape against an adapter's capability matrix}
\usage{
infer_recipe(
  shape = NULL,
  geometry = NULL,
  relation = NULL,
  design = NULL,
  targets = "default",
  adapter = NULL,
  strict = TRUE
)
}
\arguments{
\item{shape}{A \code{multifer_typed_shape} (see \code{\link[=typed_shape]{typed_shape()}}).  When
supplied, the individual \code{geometry}, \code{relation}, and \code{design}
arguments are ignored.}

\item{geometry}{Character scalar -- one of \code{"oneblock"}, \code{"cross"},
\code{"multiblock"}, \code{"geneig"}, \code{"adapter"}.  Used when \code{shape} is \code{NULL}.}

\item{relation}{Character scalar -- one of \code{"variance"},
\code{"covariance"}, \code{"correlation"}, \code{"generalized_eigen"},
\code{"predictive"}.  Used when \code{shape} is \code{NULL}.  May be \code{NULL} if the
adapter has exactly one relation for the given geometry (strict mode
errors if it has more).}

\item{design}{A \code{multifer_design} object (see \code{\link[=exchangeable_rows]{exchangeable_rows()}},
\code{\link[=paired_rows]{paired_rows()}}, \code{\link[=blocked_rows]{blocked_rows()}}, \code{\link[=nuisance_adjusted]{nuisance_adjusted()}}).  Used
when \code{shape} is \code{NULL}.  If \code{NULL}, a sensible default is chosen
based on \code{geometry}: \code{"oneblock"}, \code{"multiblock"}, and \code{"geneig"}
get \code{\link[=exchangeable_rows]{exchangeable_rows()}}; \code{"cross"} gets \code{\link[=paired_rows]{paired_rows()}}.}

\item{targets}{Character vector of requested inferential targets, or
the special sentinel \code{"default"}.  The default set is every target
the adapter declares for this \verb{(geometry, relation)} pair, excluding
\code{"variable_significance"} (deferred to Phase 3, Part 5 section 38).}

\item{adapter}{Either a \code{multifer_adapter} object or a character
scalar naming a registered adapter (looked up via
\code{\link[=get_infer_adapter]{get_infer_adapter()}}).}

\item{strict}{Logical scalar.  \code{TRUE} (default) enforces strict
dispatch (Part 5 section 35); \code{FALSE} relaxes the ambiguous-relation
rule only -- unsupported targets and \code{variable_significance} still
error unconditionally.}
}
\value{
An object of class \code{multifer_infer_recipe} with fields:
\code{problem}, \code{shape}, \code{targets}, \code{adapter_id}, \code{adapter},
\code{validity_level}, \code{strict}, \code{downgrade_reason},
\code{declared_assumptions}, \code{checked_assumptions}, \code{call}. The
\code{problem} field is the first-class compiled inferential object and
is the preferred place to inspect the inferred shape, target
family, engine kind, and validity metadata in one place.
}
\description{
\code{infer_recipe()} is the compile-time validity gate (Part 5 section 36,
point 2). It takes a typed shape -- the \verb{(geometry, relation, design)}
triple -- and a registered adapter, checks that every requested target
is declared supported for that \verb{(geometry, relation)} pair, and returns
a compiled recipe object that downstream inference can execute without
repeating these checks.
}
\section{Strict dispatch (Part 5 section 35)}{

With \code{strict = TRUE} (default) the constructor errors on silent
ambiguity: if \code{relation} is \code{NULL} and the adapter supports multiple
relations for the requested \code{geometry}, the caller must disambiguate.
With \code{strict = FALSE} the ambiguity is resolved automatically (first
declared relation wins), a \code{warning()} is emitted, and the recipe's
\code{validity_level} is downgraded one step.
}

\seealso{
\code{\link[=infer_problem]{infer_problem()}}
}
