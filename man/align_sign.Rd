% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alignment.R
\name{align_sign}
\alias{align_sign}
\title{Sign-align columns of a matched loading matrix}
\usage{
align_sign(Vref, Vb_matched)
}
\arguments{
\item{Vref}{Numeric matrix. Reference loading matrix, n x k.}

\item{Vb_matched}{Numeric matrix. Loading matrix already permuted to match
\code{Vref} (e.g. output of \code{Vb[, match_components(Vref, Vb)]}).
Must have the same dimensions as \code{Vref}.}
}
\value{
A numeric matrix of the same dimensions as \code{Vb_matched} with
sign-corrected columns.
}
\description{
For each column \code{j}, flips the sign of \code{Vb_matched[, j]} if
the inner product with \code{Vref[, j]} is negative, so that
\code{diag(t(Vref) \%*\% aligned)} is everywhere non-negative.
}
