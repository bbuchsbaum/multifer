% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter_multivarious_cca.R
\name{adapter_multivarious_cca}
\alias{adapter_multivarious_cca}
\title{multifer adapter for multivarious::cca}
\usage{
adapter_multivarious_cca(
  adapter_id = "multivarious_cca",
  adapter_version = "0.0.1",
  ncomp = NULL,
  preproc_x = NULL,
  preproc_y = NULL,
  lambda = 1e-04,
  lambda_x = NULL,
  lambda_y = NULL
)
}
\arguments{
\item{adapter_id}{Character, default \code{"multivarious_cca"}.}

\item{adapter_version}{Character, default \code{"0.0.1"}.}

\item{ncomp}{Integer, number of components to keep when refitting.
Default \code{NULL} lets the constructor pick a sensible value.}

\item{preproc_x}{Preprocessor for the X block. Default \code{NULL}
delegates to \code{multivarious::cca()}.}

\item{preproc_y}{Preprocessor for the Y block. Default \code{NULL}
delegates to \code{multivarious::cca()}.}

\item{lambda}{Shared ridge shrinkage passed through to
\code{multivarious::cca()} when block-specific lambdas are not
supplied.}

\item{lambda_x}{Optional X-block ridge shrinkage.}

\item{lambda_y}{Optional Y-block ridge shrinkage.}
}
\value{
A \code{multifer_adapter}.
}
\description{
A Tier-2 adapter wrapping \code{multivarious::cca} for the
(cross, correlation) shape. \pkg{multivarious} is a hard dependency
of \pkg{multifer}. This adapter is available as an explicit CCA
backend; \code{\link[=infer_cca]{infer_cca()}} still defaults to the
dedicated \code{"cancor_cross"} adapter.
}
\details{
This is a SINGLE-relation adapter. It declares only correlation
because canonical correlation is fundamentally a correlation-mode
method. Users wanting covariance-mode inference on cross data should
use \code{adapter_cross_svd()} or \code{adapter_multivarious_plsc()}.

Declares \code{component_significance}, \code{variable_stability},
\code{score_stability}, and \code{subspace_stability} for
(cross, correlation). Does NOT claim \code{variable_significance}
(deferred to Phase 3 per Part 5 section 38).
}
