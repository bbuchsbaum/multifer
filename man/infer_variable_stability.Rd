% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infer_result.R
\name{infer_variable_stability}
\alias{infer_variable_stability}
\title{Build the \code{variable_stability} table}
\usage{
infer_variable_stability(
  unit_id = character(0),
  domain = character(0),
  variable = character(0),
  estimate = numeric(0),
  stability = numeric(0),
  selection_freq = numeric(0),
  weight_sensitivity = numeric(0)
)
}
\arguments{
\item{unit_id}{Character vector referencing \code{units$unit_id}.}

\item{domain}{Character vector, the block label (e.g. \code{"X"}, \code{"Y"},
\code{"block_1"}).}

\item{variable}{Character vector, variable name or id within the
domain.}

\item{estimate}{Point estimate of the loading (numeric).}

\item{stability}{Stability measure (numeric, typically in \verb{[0, 1]}).}

\item{selection_freq}{Optional numeric; \code{NA_real_} if not used.}

\item{weight_sensitivity}{Optional numeric; \code{NA_real_} if not used.}
}
\value{
A data.frame with an \code{multifer_variable_stability} S3 class.
}
\description{
Keyed by \verb{(unit_id, domain, variable)}. Carries stability measures,
not p-values. Variable significance is \strong{not} part of v1
(Part 5 section 38).
}
