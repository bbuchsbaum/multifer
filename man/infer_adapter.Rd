% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter.R
\name{infer_adapter}
\alias{infer_adapter}
\title{Adapter for plugging an external model into the multifer inference engine}
\usage{
infer_adapter(
  adapter_id,
  adapter_version = "0.0.1",
  shape_kinds,
  capabilities,
  ...,
  validity_level,
  declared_assumptions = character(0),
  checked_assumptions = list(),
  component_execution = "default",
  geneig_deflation = "euclidean"
)
}
\arguments{
\item{adapter_id}{Non-empty character scalar.  Unique identifier used as
the registry key.}

\item{adapter_version}{Character scalar, e.g. \code{"0.0.1"}.}

\item{shape_kinds}{Character vector.  Subset of
\code{c("oneblock", "cross", "multiblock", "geneig")}.  Declares which
geometry families this adapter handles.}

\item{capabilities}{A \code{multifer_capability_matrix} (see
\code{\link[=capability_matrix]{capability_matrix()}}).  Every \verb{(geometry, relation, target)} triple
this adapter claims to support.}

\item{...}{Hook functions, passed by name.  See Details for the full list.}

\item{validity_level}{One of \code{c("exact", "conditional", "asymptotic", "heuristic")}.}

\item{declared_assumptions}{Character vector.  Trust-the-user assumptions
(noise model, design correctness, etc.).}

\item{checked_assumptions}{Named list of runtime-testable assumption checks.
Each element should be a list with fields \code{name} (character),
\code{check} (function(x, data) returning logical), and \code{detail} (character).}

\item{component_execution}{Character scalar. Set to \code{"adapter"} to
route component-significance through adapter-owned \code{component_stat},
\code{null_action}, and \code{residualize} hooks for geometries that otherwise
have a built-in reference engine.}

\item{geneig_deflation}{Character scalar. For adapters claiming
\verb{(geneig, generalized_eigen, component_significance)}, declare whether
the \code{residualize} hook performs \code{"b_metric"} deflation itself or
\code{"delegated"} deflation through the geneig engine.}
}
\value{
An object of class \code{multifer_adapter}.
}
\description{
An \code{infer_adapter} is the contract object that lets external packages (or
advanced users) register a model class with \code{multifer} without inheriting
from any \code{multivarious} class.  It carries three things:
}
\details{
\enumerate{
\item \strong{Capability declarations} -- a \code{multifer_capability_matrix} listing
every \verb{(geometry, relation, target)} triple this adapter claims to
support.
\item \strong{Hook functions} -- R functions the engine calls at runtime.
\item \strong{Validity metadata} -- \code{validity_level}, \code{declared_assumptions}, and
\code{checked_assumptions} (Part 5 section 36).
}

\strong{Hook functions} (all passed via \code{...}, all optional except as required by
the capability gate):
\itemize{
\item \code{roots(x)} -- numeric vector of latent roots.
\item \code{scores(x, domain)} -- matrix of scores.
\item \code{loadings(x, domain)} -- matrix of loadings.
\item \code{domains(x, data)} -- optional character vector of loading/score domains.
\item \code{project_scores(x, data, domain)} -- optional adapter-native projection of
original data through a bootstrap fit for score stability.
\item \code{truncate(x, k)} -- truncated fit.
\item \code{residualize(x, k, data)} -- deflated residual after removing k components.
\item \code{refit(x, new_data)} -- refit on perturbed data (slow-path fallback).
\item \code{refit_data(x, null_payload, original_data)} -- optional translator from
an adapter-owned \code{null_action()} payload to the \code{new_data} object expected
by \code{refit()} for null-calibrated feature-importance refits.
\item \code{bootstrap_action(x, data, design, replicate)} -- optional adapter-owned
perturbation hook returning a replicate fit or replicate data.
\item \code{core(x, data)} -- optional fast-path core representation.
\item \code{update_core(core_obj, ...)} -- optional fast-path core update.
\item \code{align(...)} -- optional adapter-owned bootstrap alignment for loadings
and scores using the named alignment signature.
\item \code{null_action(x, data)} -- generate one null resample.
\item \code{component_stat(x, data, k, split = NULL)} -- per-component test statistic.
\item \code{predict_response(x, new_data, k = NULL)} -- fitted responses for
predictive cross-family adapters.
\item \code{variable_stat(x, data, domain, k)} -- variable-level statistic.
\item \code{score_stat(x, data, domain, k)} -- score-level statistic.
}

\strong{Capability gate rules} (Part 5 section 36, point 1):
\itemize{
\item \code{component_significance} requires \code{null_action}, \code{component_stat}, AND
\code{residualize}. For \verb{(geneig, generalized_eigen)},
\code{geneig_deflation} must be \code{"b_metric"} or \code{"delegated"}. For
\verb{(cross, predictive)}, the adapter must also provide \code{refit},
\code{predict_response}, and a split-aware \code{component_stat(..., split = NULL)}.
\item \code{variable_stability} requires at least one of
\verb{\{refit, bootstrap_action, core+update_core\}} AND
(\code{variable_stat} OR \code{loadings}).
\item \code{score_stability} requires at least one of
\verb{\{refit, bootstrap_action, core+update_core\}}, \code{loadings}, AND
(\code{scores} OR \code{project_scores}).
\item \code{subspace_stability} requires at least one of
\verb{\{refit, bootstrap_action, core+update_core\}} AND \code{loadings}.
\item \code{variable_significance} is excluded from v1 (Part 5 section 38) and
always errors.
}
}
\section{Registration-time gate (Part 5 section 36, point 1)}{

The constructor enforces that every claimed capability is backed by the
required hooks.  Claiming a capability without the hooks is a registration
error that fires immediately, before any data is touched.
}
