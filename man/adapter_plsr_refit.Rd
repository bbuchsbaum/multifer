% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter_plsr.R
\name{adapter_plsr_refit}
\alias{adapter_plsr_refit}
\title{multifer adapter for pls::plsr}
\usage{
adapter_plsr_refit(
  adapter_id = "plsr_refit",
  adapter_version = "0.0.1",
  ncomp = NULL,
  method = c("simpls", "oscorespls", "kernelpls", "widekernelpls")
)
}
\arguments{
\item{adapter_id}{Character, default \code{"plsr_refit"}.}

\item{adapter_version}{Character, default \code{"0.0.1"}.}

\item{ncomp}{Optional integer cap on fitted components. When \code{NULL},
the refit hook chooses \code{min(nrow(X) - 1, ncol(X), ncol(Y), 50L)}.}

\item{method}{Character PLS algorithm passed to \code{\link[pls:mvr]{pls::plsr()}}.
One of \code{"simpls"} (default), \code{"oscorespls"}, \code{"kernelpls"}, or
\code{"widekernelpls"}. Validated via \code{\link[=match.arg]{match.arg()}} at construction time;
unknown values error before any fitting happens. The \code{"model.frame"}
value accepted by \code{pls::plsr()} itself is excluded because it returns
a model frame rather than a fitted model.}
}
\value{
A \code{multifer_adapter}.
}
\description{
A Tier-1 refit-only adapter wrapping \code{\link[pls:mvr]{pls::plsr()}} for the
\verb{(cross, predictive)} family. This adapter is intentionally
predictive rather than covariance-based: component significance is
understood as held-out incremental predictive gain, not as a
singular-value tail ratio. The null action permutes rows of \code{Y}
relative to fixed \code{X}, and the component statistic refuses to run
without explicit train/test split indices.
}
