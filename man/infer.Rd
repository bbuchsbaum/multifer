% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infer.R
\name{infer}
\alias{infer}
\title{Top-level inference dispatcher}
\usage{
infer(
  adapter,
  data,
  ...,
  recipe = NULL,
  geometry = NULL,
  relation = NULL,
  design = NULL,
  targets = "default",
  strict = TRUE,
  B = 1000L,
  B_total = NULL,
  mc_batch_size = 32L,
  R = 500L,
  alpha = 0.05,
  model = NULL,
  seed = NULL,
  parallel = c("sequential", "mirai", "auto"),
  fast_path = c("auto", "off"),
  auto_subspace = TRUE,
  tie_threshold = 0.01,
  return_bundle = FALSE
)
}
\arguments{
\item{adapter}{Either a \code{multifer_adapter} object or a string
id registered with \code{\link{register_infer_adapter}()}.}

\item{data}{For oneblock, a numeric matrix. For cross, a list with
elements \code{X} and \code{Y}. For multiblock, a list of aligned
numeric matrix blocks. For geneig, a data payload accepted by the
adapter's operator constructor (currently the LDA wrapper uses a
list with \code{X} and \code{y}). For adapter-owned geometry, any
payload accepted by the adapter hooks.}

\item{...}{Reserved for future use.}

\item{recipe}{Optional pre-compiled
\code{multifer_infer_recipe}; if supplied, \code{geometry},
\code{relation}, \code{design}, \code{targets}, and \code{strict}
are ignored.}

\item{geometry}{Character scalar for the geometry kind. Passed to
\code{\link{infer_recipe}()}.}

\item{relation}{Character scalar for the relation kind.}

\item{design}{A \code{multifer_design} object.}

\item{targets}{Character vector of requested targets, or
\code{"default"} to expand to all supported targets except
\code{variable_significance}. Geneig wrappers currently default to
\code{"component_significance"} until the public stability path is
added.}

\item{strict}{Logical, passed to \code{\link{infer_recipe}()}.
Default \code{TRUE}.}

\item{B}{Integer, per-rung cap on Monte Carlo draws.}

\item{B_total}{Optional integer, global Monte Carlo budget shared
across ladder rungs. Defaults to \code{B * max_steps}.}

\item{mc_batch_size}{Positive integer, Besag-Clifford batch size
within each rung. Default \code{32L}.}

\item{R}{Integer, number of bootstrap replicates.}

\item{alpha}{Numeric, significance threshold for the test ladder.}

\item{model}{Optional pre-fit object. When NULL, \code{adapter$refit(NULL, data)}
is called to produce the original fit.}

\item{seed}{Optional integer seed.}

\item{parallel}{One of \code{"sequential"}, \code{"mirai"}, \code{"auto"}. Controls
the bootstrap fan-out backend. \code{"sequential"} keeps the single-stream
Phase 1 loop. \code{"mirai"} / \code{"auto"} fan out per-replicate tasks to a
mirai daemon pool with deterministic per-task seeds.}

\item{fast_path}{One of \code{"auto"}, \code{"off"}. \code{"auto"} uses the Phase 1.5
core()/update_core() fast path when the adapter exposes it.
\code{"off"} forces the refit path, useful for correctness validation.}

\item{auto_subspace}{Logical. When \code{TRUE} (default), near-tied
roots are automatically bundled into a subspace unit via
\code{\link[=form_units]{form_units()}} \code{group_near_ties = TRUE}. Subspace units cause
\code{print.infer_result()} to lead with principal-angle stability.}

\item{tie_threshold}{Positive numeric. Relative-gap threshold used
when \code{auto_subspace = TRUE}. Default \code{0.01}.}

\item{return_bundle}{Logical. When \code{TRUE}, return an \code{infer_bundle}
containing the frozen \code{infer_result} plus reusable artifacts such as the
bootstrap artifact. Default \code{FALSE}.}
}
\value{
An \code{\link{infer_result}}.
}
\description{
The user-facing entry point for the shipped inference engine.
Given an adapter and data, runs the full pipeline:
\enumerate{
\item resolve / build a \code{\link{typed_shape}};
\item compile a recipe via \code{\link{infer_recipe}()} with
strict dispatch by default;
\item compile an executable plan and run its engine callback;
\item fit (or use the supplied) original fit, then run
\code{\link{bootstrap_fits}()} to produce a perturbation
artifact;
\item invoke \code{\link{variable_stability_from_bootstrap}()},
\code{\link{score_stability_from_bootstrap}()}, and
\code{\link{subspace_stability_from_bootstrap}()};
\item assemble the frozen \code{\link{infer_result}()} schema.
}
}
\details{
The default path runs the exact refit-first engine for component testing and
bootstrap summaries. When adapters expose \code{core()} and
\code{update_core()} hooks, \code{fast_path = "auto"} uses the Phase 1.5
core-update path for bootstrap refits; \code{fast_path = "off"} keeps the
refit path for agreement checks. Sequential Monte Carlo uses a global budget
allocator when \code{B_total} is supplied, and \code{parallel} can fan out
bootstrap replicates through the \code{mirai} backend.
}
