% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine_adapter.R
\name{run_adapter_ladder}
\alias{run_adapter_ladder}
\title{Run the generic adapter-owned sequential deflation engine}
\usage{
run_adapter_ladder(
  recipe,
  adapter,
  data,
  B = 1000L,
  B_total = NULL,
  batch_size = 32L,
  alpha = 0.05,
  max_steps = NULL,
  seed = NULL,
  auto_subspace = TRUE,
  tie_threshold = 0.01,
  original_fit = NULL,
  validate_data = NULL,
  labels = NULL
)
}
\arguments{
\item{recipe}{A compiled \code{multifer_infer_recipe}.}

\item{adapter}{A \code{multifer_adapter}.}

\item{data}{Data object accepted by the adapter hooks.}

\item{B}{Per-rung cap on Monte Carlo draws. Default \code{1000}.}

\item{B_total}{Optional integer global Monte Carlo budget shared
across ladder rungs. Defaults to \code{B * max_steps}.}

\item{batch_size}{Positive integer, Besag-Clifford batch size within
each rung. Default \code{32L}.}

\item{alpha}{Significance threshold. Default \code{0.05}.}

\item{max_steps}{Maximum number of ladder rungs. Default
\code{min(nrow(X), ncol(X)) - 1}, capped at 50 for Phase 1.}

\item{seed}{Integer or NULL.}

\item{auto_subspace}{Logical. When \code{TRUE} (default), near-tied
roots are automatically bundled into a subspace unit via
\code{\link[=form_units]{form_units()}} \code{group_near_ties = TRUE}.}

\item{tie_threshold}{Positive numeric. Relative-gap threshold used
when \code{auto_subspace = TRUE}. Default \code{0.01}.}

\item{original_fit}{Optional pre-fit object for \code{data}.}

\item{validate_data}{Optional function used to validate the data object at
the original, null, and residual rungs.}

\item{labels}{Optional named character list with \code{statistic}, \code{null}, and
\code{estimand} labels for result provenance.}
}
\value{
A list with the same fields as \code{run_oneblock_ladder()}.
}
\description{
Executes the latent-root ladder through adapter hooks. This engine is for
model families whose component statistic, null action, or residualization is
not the package's built-in Euclidean oneblock/cross reference calculation.
The adapter owns \code{component_stat()}, \code{null_action()}, and \code{residualize()};
the engine only supplies the ladder control flow.
}
\details{
At each rung, hook data are the current residual data object. The engine
calls \code{component_stat(fit, data, 1L)} and \code{residualize(fit, 1L, data)}, so
adapters should interpret \code{k = 1L} as "the leading component of the current
residual", not "remove the first global k components again".
}
