% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel.R
\name{multifer_parallel_lapply}
\alias{multifer_parallel_lapply}
\title{Parallel \code{lapply} with optional per-task seed threading.}
\usage{
multifer_parallel_lapply(
  X,
  FUN,
  ...,
  seeds = NULL,
  backend = c("auto", "mirai", "sequential")
)
}
\arguments{
\item{X}{List or vector to iterate over.}

\item{FUN}{Function; receives one element of \code{X} per call, plus any
\code{...} arguments.}

\item{...}{Additional arguments passed unchanged to \code{FUN}.}

\item{seeds}{Optional integer vector of length \code{length(X)}. If
supplied, \code{set.seed(seeds[i])} is called before the i-th task.}

\item{backend}{One of \code{"auto"}, \code{"mirai"}, \code{"sequential"}. \code{"auto"}
uses mirai when a daemon pool is available or can be initialized,
otherwise falls back to sequential.}
}
\value{
A list of length \code{length(X)}.
}
\description{
Parallel \code{lapply} with optional per-task seed threading.
}
