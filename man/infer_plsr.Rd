% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infer_wrappers.R
\name{infer_plsr}
\alias{infer_plsr}
\title{Infer PLSR-family significance and stability}
\usage{
infer_plsr(X, Y, adapter = "plsr_refit", ...)
}
\arguments{
\item{X}{Numeric matrix for the predictor block.}

\item{Y}{Numeric matrix for the response block. Must have the same
number of rows as \code{X}.}

\item{adapter}{Adapter id or object. Defaults to \code{"plsr_refit"}.}

\item{...}{Additional arguments forwarded to \code{\link[=infer]{infer()}}.}
}
\value{
An \link{infer_result}.
}
\description{
Thin convenience wrapper over \code{\link[=infer]{infer()}} for two-block predictive
models. The inferential target is \strong{cross-fitted held-out predictive
gain}, not an \verb{X^T Y} cross-covariance root. In other words,
\code{infer_plsr()} asks whether the next latent predictor improves
out-of-sample prediction of \code{Y} beyond the previous ones.
}
\details{
The default adapter is \code{"plsr_refit"}, a Tier-1 wrapper around
\code{pls::plsr()}. For the predictive-relation doctrine and the
registration-time cross-fit gate, see
\code{vignette("writing-adapters")}, especially the section
"The predictive relation is stricter on purpose".

As elsewhere in v1, \code{variable_significance} remains out of scope.
Component p-values are significance outputs; variable / score /
subspace summaries remain bootstrap stability outputs.
}
