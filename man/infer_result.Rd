% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infer_result.R
\name{infer_result}
\alias{infer_result}
\title{The frozen \code{infer_result} schema}
\usage{
infer_result(
  units,
  component_tests = infer_component_tests(),
  variable_stability = infer_variable_stability(),
  score_stability = infer_score_stability(),
  subspace_stability = infer_subspace_stability(),
  assumptions = infer_assumptions(),
  mc = infer_mc(),
  cost = infer_cost(),
  provenance = infer_provenance()
)
}
\arguments{
\item{units}{An \code{infer_units()} object.}

\item{component_tests}{An \code{infer_component_tests()} object.}

\item{variable_stability}{An \code{infer_variable_stability()} object.}

\item{score_stability}{An \code{infer_score_stability()} object.}

\item{subspace_stability}{An \code{infer_subspace_stability()} object.}

\item{assumptions}{An \code{infer_assumptions()} list.}

\item{mc}{An \code{infer_mc()} list.}

\item{cost}{An \code{infer_cost()} list.}

\item{provenance}{An \code{infer_provenance()} list.}
}
\value{
An object with class \code{infer_result}.
}
\description{
\code{infer_result()} is the output contract of every \code{infer()} call.
It is deliberately \strong{unit-centered}: every downstream table keys off
a \code{unit_id} drawn from the \code{units} table, not a raw component index.
A unit is either a singleton component (identifiable) or a subspace
(grouped tied / near-tied roots). This is what makes tied-root
handling honest by default (Part 5 section 37, section 44).

Composes the nine frozen sub-blocks and enforces foreign-key
consistency: every \code{unit_id} appearing in a downstream table must
exist in \code{units$unit_id}.
}
\details{
This schema is \strong{frozen} at the end of Phase 0. Adding or removing
fields requires an explicit design review.

Feature-level evidence is intentionally outside this frozen result
schema. Use the \code{feature_evidence_*()} sidecar APIs for bootstrap
ratios, null-calibrated feature p-values, VIP / coefficient
evidence, and aggregate feature importance. The legacy
\code{variable_significance} target remains blocked.
}
\section{Fields}{

\itemize{
\item \code{units} -- data.frame with columns \code{unit_id}, \code{unit_type}
(\code{"component"} or \code{"subspace"}), \code{members} (list-column of integer
vectors), \code{identifiable} (logical), \code{selected} (logical).
\item \code{component_tests} -- per-unit significance row: statistic, p-value,
Monte Carlo uncertainty, null label, validity level.
\item \code{variable_stability} -- keyed by \code{unit_id} and \code{domain}.
\item \code{score_stability} -- keyed by \code{unit_id} and \code{domain}.
\item \code{subspace_stability} -- keyed by \code{unit_id}.
\item \code{assumptions} -- list with \code{declared} and \code{checked}.
\item \code{mc} -- reproducibility log for the Monte Carlo layer (Part 5 section 41).
\item \code{cost} -- cost accounting block (Part 4 section 32).
\item \code{provenance} -- adapter id, version, capability flags, call.
}
}
