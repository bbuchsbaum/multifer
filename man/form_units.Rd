% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unit_formation.R
\name{form_units}
\alias{form_units}
\title{Form latent units from a vector of roots}
\usage{
form_units(
  roots,
  selected = NULL,
  group_near_ties = FALSE,
  tie_threshold = 0.01
)
}
\arguments{
\item{roots}{Numeric vector of roots (singular values / eigenvalues),
sorted in descending order. All values must be non-negative.}

\item{selected}{Logical vector of the same length as \code{roots}, marking
which roots were declared significant by the test ladder. If \code{NULL}
(the default), all roots are treated as selected.}

\item{group_near_ties}{Logical scalar (default \code{FALSE}). When \code{TRUE},
consecutive roots whose relative gap falls below \code{tie_threshold} are
merged into a single \code{"subspace"} unit. When \code{FALSE} (the v1
default), every root becomes its own \code{"component"} unit regardless
of proximity.}

\item{tie_threshold}{Single non-negative numeric. The relative-gap
threshold used when \code{group_near_ties = TRUE}. A pair \verb{(r_i, r_\{i+1\})}
is considered tied when
\verb{(r_i - r_\{i+1\}) / max(r_i, .Machine$double.eps) < tie_threshold}.
Default \code{0.01}.}
}
\value{
A \code{multifer_units} table built via \code{\link[=infer_units]{infer_units()}}.
}
\description{
Converts a sorted vector of singular values (or eigenvalues) into a
\code{multifer_units} table. The default behaviour treats every root as an
independent singleton component. Near-tie grouping into subspace units
is available as an explicit opt-in (see \code{group_near_ties}).
}
\details{
Near-tie grouping algorithm (opt-in only): walk roots in order.
Compare each consecutive pair via the relative gap. While the gap is
below \code{tie_threshold}, extend the current group. When the gap meets or
exceeds the threshold, close the current group and open a new one.
A group of size 1 becomes a \code{"component"} unit with \code{identifiable = TRUE}; a group of size >= 2 becomes a \code{"subspace"} unit with
\code{identifiable = FALSE}. A group is \code{selected = TRUE} only when every
member root is individually selected in the caller-supplied \code{selected}
vector (a partially-selected subspace carries \code{selected = FALSE}).
}
\examples{
# Default: every root becomes its own component unit.
form_units(c(5, 3, 1))

# Opt-in grouping: roots 1 and 2 are near-tied, root 3 is isolated.
form_units(c(5.0001, 5, 1), group_near_ties = TRUE, tie_threshold = 0.01)

}
\seealso{
\code{\link[=infer_units]{infer_units()}}, \code{\link[=infer_result]{infer_result()}}
}
