% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thin_svd_cache.R
\name{thin_svd_cache}
\alias{thin_svd_cache}
\title{Thin SVD cache}
\description{
A lightweight, fingerprint-keyed cache for thin SVDs. Used by Phase 1.5
to avoid recomputing the same decomposition across pipeline stages in a
single \code{infer()} call (the original fit, the "full observed roots" pass,
and any adapter hook that needs the bases).
}
\details{
The cache is \strong{per-context}: \code{svd_cache_new()} returns a fresh
environment that callers pass explicitly. A package-level default
cache is provided for callers that do not want to thread an
environment through.

Fingerprints are computed via \code{digest::digest()} on \code{dim} + a
low-discrepancy sample of the matrix. This is cheap and correct for
idempotent re-use within a single \code{infer()} call. It is \strong{not}
intended as a cryptographic hash or as a cross-session cache key.
Cache lifetimes are bounded by the enclosing \code{infer()} call.
}
