% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infer_result.R
\name{infer_units}
\alias{infer_units}
\title{Build the \code{units} table}
\usage{
infer_units(
  unit_id = character(0),
  unit_type = character(0),
  members = list(),
  identifiable = logical(0),
  selected = logical(0)
)
}
\arguments{
\item{unit_id}{Character vector of unique unit identifiers.}

\item{unit_type}{Character vector, each element in
\code{c("component", "subspace")}.}

\item{members}{A list of integer vectors; element \code{i} gives the raw
component indices that belong to unit \code{i}. For \code{"component"} units
this is a length-1 integer vector; for \code{"subspace"} units it is
length >= 2.}

\item{identifiable}{Logical vector. \code{TRUE} means sign / Procrustes
alignment resolves the unit. Subspace units are typically \code{FALSE}.}

\item{selected}{Logical vector. \code{TRUE} means the sequential test
ladder accepted this unit as significant.}
}
\value{
A data.frame with an \code{multifer_units} S3 class.
}
\description{
Build the \code{units} table
}
