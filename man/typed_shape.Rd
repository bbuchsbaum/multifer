% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/typed_shape.R
\name{typed_shape}
\alias{typed_shape}
\title{Typed shape — the central inferential object}
\usage{
typed_shape(geometry, relation, design)
}
\arguments{
\item{geometry}{A \code{multifer_geometry} object (see \code{\link[=geometry]{geometry()}}).}

\item{relation}{A \code{multifer_relation} object (see \code{\link[=relation]{relation()}}).}

\item{design}{A \code{multifer_design} object (see \code{\link[=exchangeable_rows]{exchangeable_rows()}},
\code{\link[=paired_rows]{paired_rows()}}, \code{\link[=blocked_rows]{blocked_rows()}}, \code{\link[=nuisance_adjusted]{nuisance_adjusted()}}).}
}
\value{
An object of class \code{multifer_typed_shape}.
}
\description{
A typed shape is the triple \verb{(geometry, relation, design)}. It is the
minimum information needed to decide whether an inference procedure
is valid on a given model and dataset, and to compile a recipe that
selects the appropriate null action, test statistic, and adapter
hooks.
}
\details{
The purpose of making this a typed object (rather than a loose list
of options) is to prevent silent ambiguity: a \code{cross} geometry with
no declared relation could equally mean PLS-like or CCA-like
inference, and those have different nulls. Typed shapes force the
caller to be explicit, and \code{strict = TRUE} recipe compilation errors
on unspecified relations.

This constructor performs \strong{structural} validation only: it checks
that each slot is of the right S3 class. Compatibility between
geometry and relation (e.g. \code{cross} + \code{variance} being unusual) is
\strong{not} checked here — that belongs in recipe compilation where the
adapter capability matrix is available.
}
