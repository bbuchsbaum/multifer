% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mc_pvalue.R
\name{mc_p_value}
\alias{mc_p_value}
\title{Fixed-B Monte Carlo p-value}
\usage{
mc_p_value(
  observed_stat,
  null_gen_fn,
  B,
  alternative = c("greater", "less", "two_sided"),
  seed = NULL
)
}
\arguments{
\item{observed_stat}{Numeric scalar. The observed test statistic. Must be
finite (not NA, NaN, or Inf).}

\item{null_gen_fn}{Function (thunk). Called \code{B} times with no arguments;
each call must return a single finite numeric value.}

\item{B}{Positive integer scalar. Number of Monte Carlo null draws.}

\item{alternative}{Character scalar. One of \code{"greater"}, \code{"less"}, or
\code{"two_sided"}. Default is \code{"greater"}. For \code{"two_sided"}, the count
\code{r} is \code{sum(abs(null_values) >= abs(observed_stat))} -- the simpler
magnitude comparison, which requires no re-centering and is
appropriate when both the null distribution and the observed statistic
are already on a symmetric scale.}

\item{seed}{Integer scalar or NULL. If not NULL, \code{set.seed(seed)} is
called before drawing and the previous RNG state is restored on exit,
leaving the caller's RNG stream unchanged.}
}
\value{
A list with elements:
\describe{
\item{\code{p_value}}{Numeric scalar. \code{(1 + r) / (B + 1)}.}
\item{\code{r}}{Integer. Count of null draws as extreme as or more
extreme than \code{observed_stat}.}
\item{\code{B}}{Integer. Number of null draws, as passed.}
\item{\code{observed_stat}}{Numeric. As passed.}
\item{\code{null_values}}{Numeric vector of length \code{B}. All null draws
in draw order. Retained for diagnostics and Phase 1.5 logging.}
\item{\code{mc_se}}{Numeric. Monte Carlo standard error
\code{sqrt(p_value * (1 - p_value) / B)}.}
\item{\code{alternative}}{Character. The chosen alternative.}
\item{\code{seed}}{Integer or NULL. As passed.}
}
}
\description{
Computes a Monte Carlo p-value using the Phipson-Smyth formula
\code{p = (1 + r) / (B + 1)}, where \code{r} is the number of null draws at least
as extreme as the observed statistic. The full null draw record is
returned so that downstream sequential-stopping logs (Phase 1.5) can
consume it without re-running the simulation.
}
\details{
The two-sided counting rule used here counts the number of null
draws whose absolute value is at least as large as the absolute
value of the observed statistic -- that is,
\code{sum(abs(null_values) >= abs(observed_stat))}.
This avoids computing a null mean and is appropriate when the null
distribution is symmetric around zero. If the null is not symmetric,
callers should use \code{"greater"} or \code{"less"} explicitly.
}
\references{
Phipson B and Smyth GK (2010). "Permutation p-values should never be
zero: calculating exact p-values when permutations are randomly drawn."
Statistical Applications in Genetics and Molecular Biology, 9(1).
}
