% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/budget_allocator.R
\name{mc_budget_allocator}
\alias{mc_budget_allocator}
\title{Sequential-MC budget allocator}
\usage{
mc_budget_allocator(B_total, per_rung_cap = NULL)
}
\arguments{
\item{B_total}{Positive integer. The global budget for the run.}

\item{per_rung_cap}{Optional positive integer. Hard cap on how many
draws any single rung can check out in one call. \code{NULL} means no
cap beyond the pool balance.}
}
\value{
A \code{multifer_budget_allocator} object: an environment with
methods \code{checkout(request)}, \code{return_unused(n)}, \code{remaining()},
\code{total()}, \code{used()}, and \code{schedule()}.
}
\description{
A lightweight state machine that holds a global Monte-Carlo budget and
lets the sequential-ladder driver draw per-rung caps that cannot exceed
the remaining pool. Budget released by rungs that decide early rolls
forward to the next undecided rung -- the "budget allocator" framing
from §28: easy components release unused draws to the boundary
component.
}
\details{
Because Vitale-style deflation forces rungs to run sequentially (rung
\code{a} depends on deflations through rung \code{a-1}), the allocator's current
role is simpler than the fully parallel §28 sketch: it is a rolling
pool that each rung checks out a cap from and reports back unused
remainder to.
}
