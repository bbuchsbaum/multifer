% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine_predictive.R
\name{run_predictive_ladder}
\alias{run_predictive_ladder}
\title{Run the predictive sequential deflation engine}
\usage{
run_predictive_ladder(
  recipe,
  X,
  Y,
  folds = NULL,
  n_folds = 5L,
  B = 1000L,
  B_total = NULL,
  batch_size = 32L,
  alpha = 0.05,
  max_steps = NULL,
  seed = NULL,
  auto_subspace = TRUE,
  tie_threshold = 0.01
)
}
\arguments{
\item{recipe}{A compiled \code{multifer_infer_recipe}. Must have geometry
\code{"cross"} and relation \code{"predictive"}.}

\item{X}{Numeric matrix, \verb{n x p}.}

\item{Y}{Numeric matrix, \verb{n x q}. Must have the same number of rows
as \code{X}.}

\item{folds}{Optional fold assignment vector of length \code{n}. When
\code{NULL}, a balanced fold assignment is created internally.}

\item{n_folds}{Positive integer. Used only when \code{folds = NULL}.
Default \code{5L}.}

\item{B}{Per-rung cap on Monte Carlo draws.}

\item{B_total}{Optional integer global Monte Carlo budget shared
across ladder rungs. Defaults to \code{B * max_steps}.}

\item{batch_size}{Positive integer, Besag-Clifford batch size within
each rung. Default \code{32L}.}

\item{alpha}{Significance threshold.}

\item{max_steps}{Maximum ladder rungs. Default
\code{min(nrow(X) - 1, ncol(X), ncol(Y), 50L)}.}

\item{seed}{Integer or NULL.}

\item{auto_subspace}{Logical. Passed through to \code{\link[=form_units]{form_units()}}.}

\item{tie_threshold}{Positive numeric. Passed through to \code{\link[=form_units]{form_units()}}.}
}
\value{
A list with \code{units}, \code{component_tests}, \code{roots_observed}, and
\code{ladder_result}, mirroring \code{\link[=run_oneblock_ladder]{run_oneblock_ladder()}} and
\code{\link[=run_cross_ladder]{run_cross_ladder()}}.
}
\description{
Implements the test ladder for \verb{(cross, predictive)} recipes via the
shared \code{\link[=ladder_driver]{ladder_driver()}}. The observed statistic at each rung is the
cross-fitted incremental predictive gain of the leading latent
predictor on the current deflated data. Gains are computed out of
sample over user-supplied folds (or a deterministic balanced K-fold
partition when \code{folds = NULL}).
}
\details{
Predictive relations are ordered by rung rather than by descending
latent-root magnitude. The returned \code{roots_observed} vector therefore
records the held-out gain sequence actually evaluated by the ladder,
while unit formation uses a strictly decreasing proxy so the shared
\code{\link[=form_units]{form_units()}} machinery can preserve rung order without implying a
latent-root interpretation.
}
