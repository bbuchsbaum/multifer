% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter_cross_baser.R
\name{adapter_cross_svd}
\alias{adapter_cross_svd}
\title{Adapter: thin-SVD cross-block adapter for (cross, covariance) and (cross, correlation)}
\usage{
adapter_cross_svd(adapter_id = "cross_svd", adapter_version = "0.0.1")
}
\arguments{
\item{adapter_id}{Character scalar. Registry key.}

\item{adapter_version}{Character scalar version string.}
}
\value{
A \code{multifer_adapter} object.
}
\description{
\code{adapter_cross_svd()} wraps a thin SVD of the cross-product matrix and
declares support for BOTH \verb{(cross, covariance)} AND \verb{(cross, correlation)}.
This dual declaration is the canonical example of the strict-dispatch
ambiguity story (Part 5 section 35): calling \code{infer_recipe()} with
\code{geometry = "cross"} and no \code{relation} on this adapter will error under
\code{strict = TRUE}.
}
\section{Fit object convention}{

The fit object produced and consumed by every hook is a plain list with
the following fields:
\itemize{
\item \code{relation} -- character scalar, either \code{"covariance"} or
\code{"correlation"}. Set by \code{refit()} from \code{new_data$relation}.
\item \code{d} -- numeric vector of latent roots (singular values of the
relation-appropriate cross matrix, length r).
\item \code{Wx} -- numeric matrix (p x r). X weight vectors (right singular
vectors of the cross matrix, or right singular vectors of the
orthonormal cross matrix for correlation mode).
\item \code{Wy} -- numeric matrix (q x r). Y weight vectors.
\item \code{Tx} -- numeric matrix (n x r). X scores: \code{X \%*\% Wx}.
\item \code{Ty} -- numeric matrix (n x r). Y scores: \code{Y \%*\% Wy}.
\item \code{center_x} -- numeric vector (p). Column means of X used for
centering.
\item \code{center_y} -- numeric vector (q). Column means of Y used for
centering.
}

The \code{relation} field governs which math path is taken: covariance mode
is the PLSC path (\code{svd(t(X) \%*\% Y)}) and correlation mode is the CCA
path (\code{svd(t(Qx) \%*\% Qy)} where Qx, Qy are thin QR orthonormal
factors). Under row permutation P, the permuted covariance root is
\code{s_a(t(X) \%*\% P \%*\% Y)} and the permuted correlation root is
\code{s_a(t(Qx) \%*\% P \%*\% Qy)}. Phase 1.5 can exploit the small-problem
structure this induces; in Phase 1 (refit-first) we use the full
recomputation path.
}

