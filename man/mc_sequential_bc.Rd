% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mc_sequential.R
\name{mc_sequential_bc}
\alias{mc_sequential_bc}
\title{Sequential Monte Carlo p-value with Besag-Clifford early stopping}
\usage{
mc_sequential_bc(
  observed_stat,
  null_gen_fn,
  B_max,
  alpha,
  h = NULL,
  batch_size = 32L,
  alternative = c("greater", "less", "two_sided"),
  seed = NULL
)
}
\arguments{
\item{observed_stat}{Numeric scalar. Observed test statistic.}

\item{null_gen_fn}{Function (thunk) returning one finite numeric
null draw per call.}

\item{B_max}{Positive integer. Hard cap on null draws.}

\item{alpha}{Significance threshold in (0, 1).}

\item{h}{Positive integer, the Besag-Clifford threshold. If \code{NULL},
defaults to \code{ceiling(alpha * (B_max + 1))}.}

\item{batch_size}{Positive integer. Draws per batch. Smaller batches
stop earlier (finer granularity); larger batches amortize RNG
overhead. Default \code{32L}.}

\item{alternative}{One of \code{"greater"}, \code{"less"}, \code{"two_sided"}.}

\item{seed}{Optional integer. If supplied, the RNG state is saved
and restored on exit.}
}
\value{
A list with elements \code{p_value}, \code{r}, \code{drawn}, \code{B_max}, \code{h},
\code{stop_reason} (one of \code{"non_reject_early"}, \code{"reject_early"},
\code{"exhausted"}), \code{batch_schedule}, \code{null_values}, \code{mc_se},
\code{alternative}, \code{seed}.
}
\description{
Draws null statistics in batches and stops as soon as the decision is
forced, giving an anytime-valid p-value that matches \code{mc_p_value()}'s
Phipson-Smyth formula \code{(1 + r) / (draws_used + 1)} on the actual draws
taken.
}
\details{
Classical Besag-Clifford early termination fires on the non-rejection
side only: when the exceedance count \code{r} reaches \code{h}, the current
p-value has already crossed above \code{alpha}, so we stop and report
\code{(1 + r) / (drawn + 1)}. Otherwise the loop runs to \code{B_max} and
returns the standard Phipson-Smyth p-value \code{(1 + r) / (B_max + 1)}.
The rejection path is kept at full budget because the reported
numeric p-value is consumed by downstream calibration checks that
compare against the fixed-B Phipson-Smyth value -- early rejection
would change the reported number without changing the decision.

The default threshold \code{h = ceiling(alpha * (B_max + 1))} is the
smallest \code{r} at which a Phipson-Smyth p-value \code{(1 + r)/(B_max + 1)}
would exceed \code{alpha}, making Besag-Clifford non-rejection exactly
align with the fixed-B rule.
}
\seealso{
\code{\link[=mc_p_value]{mc_p_value()}} for the fixed-B primitive.
}
