% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter_cross_baser.R
\name{adapter_cancor}
\alias{adapter_cancor}
\title{Adapter: stats::cancor for (cross, correlation)}
\usage{
adapter_cancor(adapter_id = "cancor_cross", adapter_version = "0.0.1")
}
\arguments{
\item{adapter_id}{Character scalar. Registry key.}

\item{adapter_version}{Character scalar version string.}
}
\value{
A \code{multifer_adapter} object.
}
\description{
Wraps \code{stats::cancor()}. Declares ONLY \verb{(cross, correlation)}, so it has
no ambiguity under strict dispatch. Use \code{adapter_cross_svd()} when you
need both covariance and correlation modes from a single adapter.
}
\section{Fit object convention}{

The fit object produced and consumed by every hook is a plain list with
the following fields:
\itemize{
\item \code{cor} -- numeric vector of canonical correlations.
\item \code{xcoef} -- numeric matrix (p x r). X canonical coefficients.
\item \code{ycoef} -- numeric matrix (q x r). Y canonical coefficients.
\item \code{xcoef_scores} -- numeric matrix (n x r). \code{X \%*\% xcoef}.
\item \code{ycoef_scores} -- numeric matrix (n x r). \code{Y \%*\% ycoef}.
\item \code{xcenter} -- numeric vector (p). X column means.
\item \code{ycenter} -- numeric vector (q). Y column means.
}
}

