% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linear_operator.R
\name{linear_operator}
\alias{linear_operator}
\title{Linear operator interface}
\usage{
linear_operator(
  apply,
  apply_t,
  dim,
  gram = NULL,
  core_update = NULL,
  repr = NULL
)
}
\arguments{
\item{apply}{Function. Applies the operator to a matrix.}

\item{apply_t}{Function. Applies the transposed operator.}

\item{dim}{Integer vector of length 2: \code{c(nrow, ncol)}.}

\item{gram}{Optional function returning \code{t(op) \%*\% op}.}

\item{core_update}{Optional function for Phase 1.5 core-space updates.}

\item{repr}{Optional list of internal data the operator wraps (useful
for debugging and for \code{as.matrix()} materialization).}
}
\value{
An object of class \code{multifer_linear_operator}.
}
\description{
Minimum surface (from Part 4 section 33) that the Phase 1.5 fast path
and core-space updates consume. A \code{multifer_linear_operator} exposes
\code{apply}, \code{apply_t}, \code{dim}, and optionally \code{gram} and \code{core_update}.
The interface is deliberately tiny so that dense matrices, sparse
matrices, implicit residualizers, kernel operators, and (later)
randomized sketches all satisfy it.
}
\details{
This is an interface, not an optimization in itself. It exists so
that \code{core()} / \code{update_core()} hooks in Phase 1.5 adapters can
accept "operator-shaped" originals without forcing a dense materialization.

Contract:
\describe{
\item{\code{apply(v)}}{\verb{n x m} operator times \verb{p x k} matrix. Returns \verb{n x k}.}
\item{\code{apply_t(v)}}{Transposed apply: \verb{p x n} times \verb{n x k}. Returns \verb{p x k}.}
\item{\code{dim()}}{Integer vector \code{c(n, p)}.}
\item{\code{gram()}}{Optional. Returns \verb{p x p} Gram matrix \code{t(op) \%*\% op}.
If not supplied, callers compute it via \code{apply_t(apply(I))}.}
\item{\code{core_update(indices, ...)}}{Optional. Advisory hook used by
Phase 1.5 adapters to accelerate row-indexed perturbations.}
}
}
