% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature_importance.R
\name{feature_importance_from_bootstrap}
\alias{feature_importance_from_bootstrap}
\title{Feature importance from a bootstrap artifact}
\usage{
feature_importance_from_bootstrap(
  artifact,
  units,
  k = c("selected", "all"),
  weights = c("equal", "root", "root2"),
  roots = NULL,
  normalize = c("block", "global", "none"),
  top_m = NULL,
  probs = c(0.025, 0.975),
  scope = c("aggregate", "unit", "both"),
  original_fit = NULL,
  adapter = NULL
)
}
\arguments{
\item{artifact}{A \code{multifer_bootstrap_artifact}.}

\item{units}{A \code{multifer_units} table.}

\item{k}{Unit selection. One of \code{"selected"}, \code{"all"}, a
character vector of unit ids, or an integer vector of unit rows.
\code{"selected"} means \code{units$selected == TRUE}.}

\item{weights}{One of \code{"equal"}, \code{"root"}, \code{"root2"},
or a numeric vector matching either all units or the selected units.}

\item{roots}{Optional numeric vector of component roots used for
\code{"root"} and \code{"root2"} weights.}

\item{normalize}{One of \code{"block"}, \code{"global"}, or
\code{"none"}.}

\item{top_m}{Optional positive integer. When supplied,
\code{top_m_frequency} is the fraction of bootstrap replicates in
which the feature rank is at most \code{top_m}.}

\item{probs}{Numeric length-2 interval probabilities.}

\item{scope}{One of \code{"aggregate"}, \code{"unit"}, or \code{"both"}.}

\item{original_fit}{Optional original adapter fit. When supplied with
\code{adapter}, the returned \code{estimate} is computed from the
original fit; otherwise it is the bootstrap mean.}

\item{adapter}{Optional \code{multifer_adapter} used with
\code{original_fit}.}
}
\value{
A \code{multifer_feature_importance} data frame.
}
\description{
Summarize feature-level importance from an existing
\code{\link{bootstrap_fits}()} artifact. This is a descriptive
bootstrap consumer: it does not refit models, does not run another
bootstrap, and does not report p-values.
}
